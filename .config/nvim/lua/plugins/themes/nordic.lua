return {
  "AlexvZyl/nordic.nvim",
  enabled = false, -- Disabled: using catppuccin
  --lazy = false,
  priority = 1000,
  config = function()
    --require("nordic").load()
    --vim.cmd.colorscheme("nordic")
  end,
}
